<IfModule mod_rewrite.c>
    RewriteEngine On

    # -------------------------
    # Redirect all requests to /public
    # -------------------------
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
